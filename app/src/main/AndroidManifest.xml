<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions for streaming -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Wake lock for continuous playback -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Audio focus for media playback -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- For Android TV and leanback -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" 
        tools:ignore="ProtectedPermissions" />
    
    <!-- Optional: For analytics and crash reporting -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" 
        android:required="false" />

    <application
        android:name=".DreamsTVApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.DreamsTV"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true"
        android:largeHeap="true">
        
        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DreamsTV"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Test Streaming Activity -->
        <activity
            android:name=".TestStreamingActivity"
            android:exported="false"
            android:theme="@style/Theme.DreamsTV"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        
        <!-- TVMaze Test Activity -->
        <activity
            android:name=".TVMazeTestActivity"
            android:exported="false"
            android:theme="@style/Theme.DreamsTV"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        
        <!-- Media Session Service for background playback -->
        <service
            android:name=".service.MediaPlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
            
        <!-- Media Browser Service for Android TV -->
        <service
            android:name=".service.MediaBrowserService"
            android:exported="true"
            android:permission="android.permission.BIND_GET_INSTALL_REFERRER_SERVICE">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
        
        <!-- File Provider for sharing content -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>
</manifest>