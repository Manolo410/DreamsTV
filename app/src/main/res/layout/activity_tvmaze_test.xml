<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background_dark"
    android:padding="16dp">

    <!-- Header -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/tvmaze_test_title"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:gravity="center"
        android:padding="16dp"
        android:background="@color/primary_blue" />

    <!-- Search Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp">

        <EditText
            android:id="@+id/tvmaze_search_edit"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="@string/tvmaze_search_hint"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_hint"
            android:background="@color/surface_medium"
            android:padding="12dp"
            android:layout_marginEnd="8dp"
            android:autofillHints="search"
            android:inputType="text" />

        <Button
            android:id="@+id/tvmaze_search_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tvmaze_search_button"
            android:textColor="@color/white"
            android:background="@drawable/button_primary_background"
            android:layout_marginEnd="4dp" />

    </LinearLayout>

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        style="?android:attr/buttonBarStyle">

        <Button
            android:id="@+id/tvmaze_schedule_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:text="@string/tvmaze_schedule_button"
            android:textColor="@color/primary_blue"
            android:background="@drawable/button_secondary_background"
            style="?android:attr/buttonBarButtonStyle" />

        <Button
            android:id="@+id/tvmaze_people_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:text="@string/tvmaze_people_button"
            android:textColor="@color/primary_blue"
            android:background="@drawable/button_secondary_background"
            style="?android:attr/buttonBarButtonStyle" />

    </LinearLayout>

    <!-- Status Text -->
    <TextView
        android:id="@+id/tvmaze_status_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/tvmaze_ready_message"
        android:textSize="14sp"
        android:textColor="@color/text_secondary"
        android:gravity="center"
        android:padding="8dp"
        android:background="@color/surface_medium" />

    <!-- Content List -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/tvmaze_results_title"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:padding="8dp" />

    <ListView
        android:id="@+id/tvmaze_content_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/surface_medium"
        android:divider="@color/outline"
        android:dividerHeight="1dp"
        android:padding="8dp" />

    <!-- TVMaze Info -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp"
        android:background="@color/surface_dark">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="TVMaze Free API Features:"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="• No API key required\n• Search TV shows and people\n• Get TV schedules\n• Cast and crew information\n• Episode details\n• Completely free!"
            android:textSize="12sp"
            android:textColor="@color/text_secondary"
            android:padding="4dp" />

    </LinearLayout>

</LinearLayout>
